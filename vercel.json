{
  "routes": [
    {
      "src": "^/(?!.*\\.)(?!api).*$",
      "dest": "/index.html"
    }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "env": {
    "OMDB_API_KEY": "@omdb_api_key"
  }
}
